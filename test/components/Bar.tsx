import React from "react";

/**
 *
 * @param param0
 * @returns
 */
const Bar = ({}: BarProps) => {
  return <>Bar</>;
};

export declare interface BarProps {
  children: React.ReactNode;
}

export default Bar;
